#### Simple test validating that transaction sending eth to a contract that doesn't have a payable fallback will revert.

##### To run first make sure there is a local ganache node running, then call `truffle test`
